import React from 'react';
import ReactDOM from 'react-dom';
import App from './reviewBox';

const root = ReactDOM.createRoot(document.getElementById('root'));

root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
export default App;